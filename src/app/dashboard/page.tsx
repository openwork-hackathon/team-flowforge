"use client";

export default function DashboardPage() {
  return (
    <main className="min-h-screen p-8">
      <header className="mb-8">
        <a href="/" className="text-lg font-bold text-blue-400">🔨 FlowForge</a>
        <span className="ml-4 text-slate-500">/ Dashboard</span>
      </header>
      <div className="text-slate-500">
        {/* Clawdia: Pipeline monitoring dashboard — see Issue #4 */}
        <p>Pipeline Dashboard — Coming soon (Issue #4)</p>
      </div>
    </main>
  );
}
